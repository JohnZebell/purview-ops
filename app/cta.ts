/* One CTA, repeated. Same label everywhere on the site.

   CTA_HREF is the cross page destination, used by the header and by any page
   that is not the home page. The home page uses a bare "#audit" for its own
   in page buttons, since the audit section lives on it. Both become /audit
   when that page ships. */
export const CTA_HREF = '/#audit'
export const CTA_LABEL = 'Start with the audit'
